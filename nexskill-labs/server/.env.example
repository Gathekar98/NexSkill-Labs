# Copy this file to .env and fill in real values before running the server.

PORT=5000

# Where lead notification emails get delivered
NOTIFY_EMAIL=ashwinigathekar2704@gmail.com

# SMTP credentials used to SEND the notification email.
# Works with Gmail (use an App Password, not your normal password),
# or any SMTP provider (SendGrid, Mailgun, Resend, Zoho, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ashwinigathekar2704@gmail.com
SMTP_PASS=hfqfgdvqybuljtsb
# Comma-separated list of frontend origins allowed to call this API
CORS_ORIGIN=http://localhost:5173
